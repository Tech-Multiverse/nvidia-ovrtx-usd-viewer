# Public web-server dependencies for the RTX USD Viewer.
# Install with:
#     pip install -r requirements.txt
# Or use the editable package install:
#     pip install -e .
#
# NOTE: this file does NOT include ovrtx, carb, omni.*, or pxr (USD).
# Those are proprietary NVIDIA packages that must be installed separately
# via NVIDIA's Omniverse / Isaac Sim conda channels.

fastapi>=0.139
uvicorn>=0.49
websockets>=16
python-multipart>=0.0.9
pillow>=12
numpy>=2
